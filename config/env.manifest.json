{
  "$comment": "LA VÉRITÉ SUR LA CONFIGURATION, DÉCLARÉE UNE FOIS. Elle existe aujourd'hui en quatre exemplaires dans chaque app — ce que le code lit, ce que .env.example documente, ce que le workflow injecte, ce que GitHub détient — et rien ne les compare. Ce fichier est celui dont les trois autres doivent dériver (cf. CONFIG.md du socle, phase 2). L'outil `pwa-env` qui l'exploite reste à écrire ; le manifeste, lui, se tient à jour dès maintenant.",
  "app": "pwa-starter-kit",
  "entries": [
    {
      "name": "VITE_SENTRY_DSN",
      "store": "vars",
      "phase": "build",
      "required": "optional",
      "purpose": "Point de collecte des erreurs.",
      "fallback": "l'observabilité se tait, et Sentry n'est pas chargé du tout",
      "provider": "sentry"
    },
    {
      "name": "VITE_BACKEND",
      "store": "vars",
      "phase": "build",
      "required": "optional",
      "purpose": "Force le backend au lieu de le déduire des variables présentes.",
      "fallback": "backend local — l'app démarre sans configuration",
      "provider": "none"
    },
    {
      "name": "VITE_SUPABASE_URL",
      "store": "vars",
      "phase": "build",
      "required": "optional",
      "purpose": "URL du projet Supabase.",
      "fallback": "backend local — la page publique marche sans compte",
      "provider": "supabase"
    },
    {
      "name": "VITE_SUPABASE_ANON_KEY",
      "store": "vars",
      "phase": "build",
      "required": "optional",
      "purpose": "Clé anonyme, publique par conception : c'est la RLS qui protège, pas sa discrétion.",
      "fallback": "backend local",
      "provider": "supabase"
    },
    {
      "name": "SUPABASE_ACCESS_TOKEN",
      "store": "secrets",
      "phase": "deploy",
      "required": "optional",
      "purpose": "Jeton personnel pour pousser les migrations.",
      "provider": "supabase",
      "rotation": "P180D"
    },
    {
      "name": "SUPABASE_PROJECT_ID",
      "store": "vars",
      "phase": "deploy",
      "required": "optional",
      "purpose": "Référence du projet, pour supabase link. Publique : elle est dans l'URL. Passée dans le slot secret du réutilisable, qui accepte une variable.",
      "provider": "supabase"
    },
    {
      "name": "SUPABASE_DB_PASSWORD",
      "store": "secrets",
      "phase": "deploy",
      "required": "optional",
      "purpose": "Mot de passe de la base, pour db push.",
      "provider": "supabase",
      "rotation": "P180D"
    }
  ],
  "$rules": [
    "Une entrée dont le nom commence par VITE_ ne peut pas avoir store: secrets — Vite la copie dans le bundle publié, et un secret n'y masque que les journaux de CI.",
    "Une entrée store: secrets ne peut pas commencer par VITE_.",
    "Une exception se déclare avec sa raison, en toutes lettres : sans quoi le chiffre de dette ne descend jamais et plus personne ne le regarde."
  ]
}
